# Archived Business-Theme Pass

These files belong to an earlier analysis-oriented workflow that extracted broad business themes and JD-to-theme matches.

They are archived because the active skill now uses a website-oriented production workflow:

```text
case index -> filter tags -> JD talent-language assets -> BTG-style case-role demos
```

Do not use these archived files as primary inputs unless the user explicitly asks to revisit the older research pass.
