# Services Index

> 状态：栏目索引占位，用于对齐 header 导航、docs 分类和 `fronted/services/` 目录。

## 1. 栏目职责

`/services` 是 header 中“我们做什么”的一级栏目入口，负责总览左安门可提供的企业端服务能力，并把具体服务导向子页面。

该页不是单个服务详情页，也不是表单页。企业提交需求仍跳转到 `/find-talent`。

## 2. 当前子页面

| 子页面 | 文档路径 | 前端路径 | 建议路由 | 状态 |
|---|---|---|---|---|
| 按需人才 | `docs/services/on-demand-talent/02-services.md` | `fronted/services/on-demand-talent/` | `/services/on-demand-talent` | 已有草稿，需要后续定稿 |
| 临时高管 | `docs/services/interim-executive/interim-executive.md` | `fronted/services/interim-executive/` | `/services/interim-executive` | 占位，待讨论 |

## 3. 栏目页模块建议

1. Global Header
2. Services Overview Hero
3. Service Category Grid
4. Typical Enterprise Scenarios
5. How Zuoan Works
6. Service Boundaries
7. Submit Demand CTA
8. Global Footer

## 4. 点击跳转

- Header `我们做什么` -> `/services`
- `按需人才` -> `/services/on-demand-talent`
- `临时高管` -> `/services/interim-executive`
- `提交需求` -> `/find-talent`

## 5. 待补内容

- 是否需要把“项目制专家团队”和“AI 增长与战略转型”拆成独立子页面。
- `/services` 栏目页是否先做轻量总览，还是直接跳转到第一个服务子页。
